# Frontend

Next.js (App Router) vitrin ve admin paneli.

Kurulum, klasör yapısı ve geliştirme kuralları için kök dizindeki [README.md](../README.md) dosyasına bakın.

```bash
cp .env.example .env.local
npm install
npm run dev -- -p 3001
```
